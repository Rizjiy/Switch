// home_Connection.h

#ifndef _HOME_CONNECTION_h
#define _HOME_CONNECTION_h

#if defined(ARDUINO) && ARDUINO >= 100
	#include "arduino.h"
#else
	#include "WProgram.h"
#endif

class Connection {
public:
	Connection();
};

#endif

